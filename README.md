
# Product Discovery with AI Assist

## Setup

### Backend
cd backend
npm install
Create .env file:
OPENAI_API_KEY=your_key
npm start

### Frontend
cd frontend
npm install
npm start

## Endpoints
GET /api/products
POST /api/ask

## LLM
Uses OpenAI with structured JSON output.
